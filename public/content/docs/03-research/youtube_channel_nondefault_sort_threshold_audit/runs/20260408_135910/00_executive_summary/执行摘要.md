# 执行摘要

## 一句话结论

本轮关键词集为 `marathon, cosmetics, swim vest`。当前最脏的非默认组是 `marathon × 按粉丝数排序`，相对最稳的是 `cosmetics × 按粉丝数排序`，召回压缩最明显的是 `swim vest × 按粉丝数排序`。按“平衡优先”判断，建议 **适度放宽**。

## 关键数字

- 最脏非默认组：`marathon × 按粉丝数排序`，clear badcase 98/100。
- 最稳非默认组：`cosmetics × 按粉丝数排序`，clear badcase 24/100。
- 最大召回压缩组：`swim vest × 按粉丝数排序`，结果数 195 -> 1。

## 对数据组的建议口径

- “是否可接受”不能只看 badcase 降了多少，还要看 recall 是否被压没了。
- 默认排序作为基线，用来判断非默认排序是否真的带来了更合理的前部结果。
- 主报告里的 clear badcase 不是主观打分，而是基于频道标签、最近视频与默认排序对照构成的证据链。
- 同一轮的小调整默认覆盖当前轮次，仅在需要横向对比另一套阈值时再开新轮次。

