# 7611 研发需求 Checklist：内容监控上线反馈修正

> 状态：`dev_checklist_draft`
> 上游：`7611_supplement_v0.md`
> Demo：`06_Prototypes/monitor_761_layout_demos/`
> 作用：给研发、测试、设计对齐 7611 补充需求的拆任务清单；不替代 `prd_v1.md`，仅覆盖 761 上线后的补充修正。

## 0. 核心口径

- [ ] 7611 是 761 的小版本补充，不重做内容监控 2.0 整体 IA。
- [ ] 工作台仍使用统一页面承载单项目、多项目、全部项目范围。
- [ ] 项目层字段继续以线上事实为准：只有 `创建人 / 协作者`，不新增项目负责人。
- [ ] 自动追踪新增结果走全站公用系统通知；内容监控页面内不新增通知模块、通知卡片或通知示例 UI。

## 1. URL 与页面入口

- [ ] 工作台继续支持 `projects=<projectId>` 进入单项目范围。
- [ ] 工作台继续支持 `projects=<projectId1>,<projectId2>` 进入多项目范围。
- [ ] 工作台继续支持 `projects=all` 进入全部可见项目范围。
- [ ] 工作台新增或确认支持 `tab=overview|dashboard`。
- [ ] `tab` 缺省值为 `overview`。
- [ ] `overview` 表示 `监控概览`，承载原始数据和监控内容表格。
- [ ] `dashboard` 表示 `数据看板`，承载观察区间分析、趋势、榜单、标签贡献等分析内容。
- [ ] 从项目列表处理型指标进入工作台时，URL 需要带入 `tab=overview`。
- [ ] 从系统通知进入工作台时，URL 需要带入 `tab=overview`、列表筛选参数和通知来源参数。

## 2. 项目列表顶部总览

### 2.1 总览定位

- [ ] 项目列表顶部总览是当前项目列表筛选结果的聚合摘要。
- [ ] 项目总览必须对下方项目列表内容负责。
- [ ] 项目总览和项目列表使用同一组筛选条件。
- [ ] 项目总览不再提供日期选择器。
- [ ] 项目总览不再展示增量主值。
- [ ] 项目总览不再拆分为 `当前快照 / 处理提醒` 两组口径。

### 2.2 总览指标

- [ ] 项目数：当前筛选结果下的项目数量。
- [ ] 观看量：当前筛选结果下所有项目监控任务的最新观看量累计值。
- [ ] 互动量：当前筛选结果下所有项目监控任务的最新互动量累计值。
- [ ] 活跃监控数：当前筛选结果下仍在有效监控中的任务数。
- [ ] 需关注内容：当前筛选结果下需处理的监控任务数。
- [ ] 自动追踪新增：当前筛选结果下自动追踪新增任务数。
- [ ] `观看量` 展示 7 天对比。
- [ ] `互动量` 展示 7 天对比。
- [ ] `活跃监控数` 展示 7 天对比。
- [ ] 7 天对比只作为辅助变化提示，不改变主值的当前值口径。
- [ ] 项目数、观看量、互动量、活跃监控数默认只展示，不做下钻。
- [ ] 需关注内容、自动追踪新增可以点击进入工作台列表筛选。

### 2.3 筛选项

- [ ] 项目总览区提供 `项目创建人` 筛选。
- [ ] 项目总览区提供 `项目状态` 筛选。
- [ ] 项目状态选项为：全部、进行中、已归档。
- [ ] 不再提供 `平台范围` 筛选。
- [ ] 不再提供 `提醒周期` 筛选。
- [ ] 不再提供项目总览专属的日期筛选。
- [ ] 筛选项同时影响项目总览数据和项目列表卡片。
- [ ] 可点击卡片静态态需要展示明确的跳转意图图标。
- [ ] 非点击型卡片不展示跳转图标。

## 3. 项目列表卡片

- [ ] 项目卡片保留整卡点击进入单项目工作台。
- [ ] 整卡点击进入 `projects=<projectId>&tab=overview`。
- [ ] 项目卡片展示当前观看。
- [ ] 项目卡片展示当前互动率。
- [ ] 项目卡片展示需关注数量。
- [ ] 项目卡片展示自动新增数量。
- [ ] 项目卡片展示当前内容数量。
- [ ] `需关注` 为处理入口，点击进入工作台并应用对应列表筛选。
- [ ] `自动新增` 为处理入口，点击进入工作台并应用对应列表筛选。
- [ ] 项目列表筛选同时影响项目卡片展示和顶部项目总览。
- [ ] 已归档项目需要与运行中项目有明确状态区分。

## 4. 工作台 TAB 布局

- [ ] 默认进入 `监控概览` TAB。
- [ ] `监控概览` 内展示原始数据块。
- [ ] `监控概览` 内展示监控内容表格。
- [ ] `数据看板` 作为第二 TAB。
- [ ] 切换 TAB 时保留当前 `projects` 范围。
- [ ] 切换 TAB 时保留一级筛选条件。
- [ ] 切换 TAB 时保留列表筛选条件，但列表筛选仅在 `监控概览` 中实际生效。
- [ ] 从项目列表或系统通知带入的列表筛选，需要在 `监控概览` 中可见、可清除。
- [ ] `监控概览` 不受数据看板的数据观察周期影响。

## 5. 筛选分层

- [ ] 一级筛选控制当前内容集合。
- [ ] 一级筛选影响数据看板、原始数据、监控内容表格和任务详情抽屉。
- [ ] 二级筛选只控制监控内容表格。
- [ ] 二级筛选用于快速处理任务，不改写数据看板统计口径。
- [ ] 数据观察周期只在数据看板内生效。
- [ ] 数据观察周期不影响原始数据块。
- [ ] 数据观察周期不影响监控内容表格。
- [ ] 从外部入口带入的 `listFilter` 只落在二级筛选层。
- [ ] 清除二级筛选后，表格回到仅受一级筛选影响的状态。

## 6. 自动追踪系统通知

### 6.1 通知发送

- [ ] 自动追踪规则抓取到新监控任务后，按小时汇总发送全站系统通知。
- [ ] 同一小时内无新增时不发送通知。
- [ ] 通知只发送给对对应项目有可见权限的用户。
- [ ] 多项目汇总通知不得暴露用户无权限项目名称。
- [ ] 通知内容至少包含项目范围、新增任务数、规则名或主要规则名、时间窗口。

### 6.2 通知跳转

- [ ] 点击通知主链接进入内容监控工作台。
- [ ] 跳转后进入 `tab=overview`。
- [ ] 跳转后应用自动追踪新增列表筛选。
- [ ] 跳转后保留 `source=notification` 或等价来源参数，便于埋点和上下文提示。
- [ ] 点击通知中的单条任务链接时，进入对应项目工作台并拉出单内容监控任务详情抽屉。
- [ ] 内容监控项目列表、工作台、数据看板内不新增系统通知示例卡片。

## 7. 项目归档与撤销归档

### 7.1 归档

- [ ] 运行中项目提供归档操作。
- [ ] 归档操作必须二次确认。
- [ ] 归档不删除项目。
- [ ] 归档不删除历史数据。
- [ ] 归档后项目从默认运行中列表退出。
- [ ] 归档后历史数据、内容列表和任务详情仍可查看。
- [ ] 归档后禁用添加监控入口。
- [ ] 归档后暂停自动追踪新增。
- [ ] 归档后已存在监控任务继续更新到原监控结束时间。

### 7.2 撤销归档

- [ ] 已归档项目提供撤销归档入口。
- [ ] 撤销归档后项目回到运行中状态。
- [ ] 撤销归档后项目恢复默认项目列表可见。
- [ ] 撤销归档后添加监控入口恢复。
- [ ] 撤销归档不自动启用已停用的自动追踪规则。
- [ ] 撤销归档后自动追踪是否继续执行，以该项目原有规则状态为准。

## 8. 数据与接口待确认

- [ ] 当前监控内容总数的数据来源已确认。
- [ ] 当前总观看量的数据来源、刷新时机和空态已确认。
- [ ] 当前总 Engagement 的组成字段已确认。
- [ ] 需关注内容的命中规则已确认。
- [ ] 自动追踪新增的统计窗口与去重规则已确认。
- [ ] 短链待处理的判定条件已确认。
- [ ] 项目归档状态字段已确认。
- [ ] 撤销归档接口或状态流转已确认。
- [ ] 自动追踪小时汇总通知的数据源、调度方式和失败重试策略已确认。
- [ ] 系统通知 deep link 参数规范已确认。
- [ ] `listFilter` 枚举值和后端查询参数映射已确认。

## 9. QA Checklist

- [ ] 默认打开工作台时首先看到 `监控概览`。
- [ ] 直接访问 `tab=dashboard` 时能进入数据看板。
- [ ] 项目列表顶部不再出现日期选择器、提醒周期或平台范围筛选。
- [ ] 项目创建人筛选会同时改变项目总览和项目列表。
- [ ] 项目状态筛选会同时改变项目总览和项目列表。
- [ ] 项目总览中的观看量、互动量、活跃监控数展示 7 天对比，但主值仍为当前值。
- [ ] 点击项目列表 `需关注内容` 后进入工作台，并只筛选监控内容表格。
- [ ] 点击项目列表 `自动追踪新增` 后进入工作台，并只筛选监控内容表格。
- [ ] 清除列表筛选后，原始数据和数据看板口径没有被改写。
- [ ] 内容监控页面内没有系统通知示例卡片或通知模块。
- [ ] 从系统通知 deep link 进入时，能打开正确项目范围和列表筛选。
- [ ] 从系统通知单条任务链接进入时，能打开单内容监控任务详情抽屉。
- [ ] 运行中项目归档前会弹出确认。
- [ ] 已归档项目可以撤销归档。
- [ ] 撤销归档后项目回到运行中列表。
- [ ] 已停用的自动追踪规则不会因为撤销归档自动启用。

## 10. 本期明确不做

- [ ] 不改新建监控流程。
- [ ] 不扩展评论抓取能力。
- [ ] 不新增报告型 PDF / 图片导出。
- [ ] 不新增项目负责人字段。
- [ ] 不在内容监控页面内设计系统通知 UI。
- [ ] 不重做自动追踪规则管理中心。
- [ ] 不在 7611 中解决自定义标签交并集交互。

## 11. 待产品确认项

- [ ] `listFilter` 的最终枚举命名。
- [ ] `需关注内容` 的完整命中规则与优先级。
- [ ] 项目总览 7 天对比的计算来源和刷新时机。
- [ ] 系统通知文案模板。
- [ ] 系统通知是否需要支持按项目聚合和按规则聚合两种展示方式。
- [ ] 当前值指标是否需要展示最近刷新时间。
