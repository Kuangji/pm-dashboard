# _plan.md · search_experience_6310

## 版本范围

- [x] 智能精选工作台定义
- [x] 输入列表快照模型
- [x] 轮次结果消费与策略调整
- [x] session 持久化与独立化
- [x] 标准搜索到智能精选桥接
- [x] 明确依赖 `v6.3.9`
- [ ] 精选策略指令与基底选择器细化
- [ ] `继续筛 / 重新筛` Demo 后回看确认

## 当前风险

### 风险 1：智能精选形态仍可能继续调整

处理：

- 将智能精选整包移出 `v6.3.9`
- 作为 `v6.3.10` 独立继续打磨

### 风险 2：桥接范围继续发散

处理：

- 本轮只锁 `标准搜索 -> 智能精选`
- 其它入口暂不纳入

### 风险 3：与 `v6.3.9` 重复定义基础能力

处理：

- 在 README 和 spec 中显式声明依赖 `v6.3.9`
- 不再重述标准搜索自然语言与相似网红主体

### 风险 4：精选交互容易过重

处理：

- 前台默认采用“半透明模式”，自然语言仍是主入口
- 不做完整策略编辑器，先用轻量策略摘要表达系统理解
- `继续筛 / 重新筛` 语义显式分开，并在 Demo 后再次确认是否需要调整
